<!Doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= settings.title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <style>
        /*login style*/
        /* Custom CSS for centering the login form */
        .login-container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        /*sidebar style*/
        /* Custom CSS for sidebar */
        .sidebar {
            position: fixed;
            top: calc(4rem );
            left: 0;
            bottom: 0;
            width: 20%;
            background-color: #f8f9fa;
            padding: 20px;
            overflow-y: auto;
        }
        .sidebar .nav-link {
            font-weight: 500;
            color: #333;
        }
        /* Adjust main content to accommodate sidebar */
        .main-content {
            margin-left: 250px; /* Adjust this value to match the width of the sidebar */
            padding-top: 60px; /* Adjust this value to match the height of the navigation bar */
            padding-left: 20px; /* Adjust this value to create space between the sidebar and main content */
        }
        /* Add custom margin to the top of navbar */
        .navbar {
            margin-bottom: 20px;
        }
    </style>
</head>
<body class="m-0">

